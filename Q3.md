```text
Finance signale que `models/marts/_refactor_me/mart_messy_revenue.sql` surestime le chiffre d'affaires.

Identifie la cause racine en 2 phrases maximum, corrige le modèle, puis lance `dbt build --select mart_messy_revenue`.

Montre ensuite uniquement un comparatif avant/après des indicateurs utiles à la démo :
- nombre de lignes,
- revenu total.
```
