```text
Migre `legacy/sp_customer_ltv.sql` vers `models/marts/finance/mart_customer_ltv.sql`.

Contraintes :
- utilise `ref()` vers les modèles `staging` et `intermediate` existants plutôt que les tables brutes,
- dialecte BigQuery,
- SQL conforme dbt Fusion (`cast(... as ...)` uniquement),
- conserve la logique métier, les seuils de segmentation et les colonnes de sortie,
- n'inclus que les clients ayant au moins une commande completed.

Ensuite, lance `dbt build --select mart_customer_ltv`, corrige automatiquement ce qui bloque si nécessaire, puis indique uniquement `succès` ou `échec`.
```
